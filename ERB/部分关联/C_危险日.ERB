;=================================================
;   sub C_危險日
;=================================================
;   危險日
;-------------------------------------------------
;   對象が危險日か返す
;-------------------------------------------------
; Input:
;  ARG:0				角色
; Output:
;  RETURNF				真偽值
;-------------------------------------------------
@C_危险日(ARG = -1)
#FUNCTION
#DIM L_TMP
#DIM L_VAL

SIF ARG == -1
	ARG = TARGET

L_TMP = CFLAG:ARG:危险日
L_VAL = (DAY % 30)

;15日に一回危險日
L_TMP /= 2
L_VAL /= 2

IF TALENT:ARG:男性
	RETURNF 0
ELSEIF L_TMP == L_VAL
	RETURNF 1
ELSE
	RETURNF 0
ENDIF